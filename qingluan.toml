[workspace]
name = "qingluan"
root = "."

[sandbox]
provider = "local"
template = "qingluan-dev"

[sync]
exclude = [
  "target",
  "node_modules",
  "dist",
  "build",
  ".git",
]

[tasks.code_review]
command = "echo code-review placeholder"

[tasks.test]
command = "cargo test --workspace"
